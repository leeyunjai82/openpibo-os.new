# 빌드 / 모델 변환 도구. **기기에 넣지 않는다.**
# 변환은 개발 머신에서 하고 결과 파일만 기기로 보낸다.
#
#   pip install -r requirements/build.txt

# wheel 빌드 (system/build_wheel.sh)
setuptools>=68
wheel>=0.42

# 모델 변환
openvino-dev>=2024.5      # 메타패키지. IR 변환 도구
onnx>=1.17
onnxslim>=0.1

# tfjs -> keras (classifier/tfjs_to_keras.py). subprocess 로 격리돼 있다.
tensorflowjs>=4.0
tensorflow-hub>=0.16
